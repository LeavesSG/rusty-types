{
    "name": "ts-playground",
    "private": true,
    "version": "0.1.1",
    "type": "module",
    "prettier": "@natsuha_sg/prettier-config",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "format": "pnpx prettier --write './src/**/*.{ts,json}'"
    },
    "devDependencies": {
        "typescript": "latest",
        "@natsuha_sg/prettier-config": "latest",
        "prettier": "latest"
    }
}
